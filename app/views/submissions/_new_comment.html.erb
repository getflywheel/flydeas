<% @comment = Comment.new %>
<%= form_for @comment, url: submission_comments_url(submission_id: @submission.id), html: {method: :post} do |f| %> 
	<%= f.text_field :content %>
	<%= f.submit "Submit comment" %>
<% end %>
